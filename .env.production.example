# ========================================
# 프로덕션 환경 설정 (.env.production)
# ========================================
# ⚠️  주의: 민감한 정보는 절대 커밋하지 마세요!
# 프로덕션 서버에서만 안전하게 설정하세요.

# Application
NODE_ENV=production
PORT=3000

# Database (프로덕션 PostgreSQL)
# AWS RDS, Azure Database, Google Cloud SQL 등 관리형 데이터베이스 권장
DATABASE_URL=postgresql://prod_user:strong_password_here@db.production.example.com:5432/sundey_crm?schema=public&sslmode=require

# JWT (강력한 시크릿 사용!)
# openssl rand -base64 32 로 생성
JWT_SECRET=YOUR_STRONG_PRODUCTION_JWT_SECRET_HERE_MIN_32_CHARS
JWT_EXPIRES_IN=1h
JWT_REFRESH_SECRET=YOUR_STRONG_PRODUCTION_REFRESH_SECRET_HERE_MIN_32_CHARS
JWT_REFRESH_EXPIRES_IN=7d

# Redis (프로덕션 Redis)
# AWS ElastiCache, Azure Cache, Google Cloud Memorystore 등 권장
REDIS_HOST=redis.production.example.com
REDIS_PORT=6379
REDIS_PASSWORD=your_strong_redis_password_here

# BullMQ
BULL_QUEUE_NAME=sundey-queue-prod

# CORS (프로덕션 도메인)
CORS_ORIGIN=https://www.yourdomain.com

# Oracle Object Storage (S3 Compatible)
OCI_NAMESPACE=your-prod-namespace
OCI_BUCKET_NAME=your-prod-bucket
OCI_REGION=ap-chuncheon-1
OCI_S3_ENDPOINT=https://your-namespace.compat.objectstorage.ap-chuncheon-1.oraclecloud.com
OCI_ACCESS_KEY_ID=your-prod-access-key
OCI_SECRET_ACCESS_KEY=your-prod-secret-key
OCI_PRIVATE_KEY_PATH=/etc/sundey/oci_api_key
OCI_TENANCY_ID=your-prod-tenancy-id
OCI_USER_ID=your-prod-user-id
OCI_FINGERPRINT=your-prod-fingerprint
